<h1>Editing photo</h1>

<div class="help-links">
  <%= link_to(raw('<i class="icon-backward"></i> Show all'), photos_path) %>
  <%= link_to(raw('<i class="icon-eye-open"></i> Show'),  @photo) %>
</div>

<%= render 'form' %>