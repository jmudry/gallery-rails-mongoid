<h1>Editing photo</h1>

<div class="help-links">
  <%= link_to(raw('<i class="icon-backward"></i> Show all'), album_photos_path([@photo.album])) %>
  <%= link_to(raw('<i class="icon-eye-open"></i> Show'),  [@photo.album, @photo]) %>
</div>

<%= render :partial => "photos/form", locals: {photo: @photo, album: @album} %>